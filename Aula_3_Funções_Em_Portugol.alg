Algoritmo "Funções em portugol"

Var
   A: real
Inicio

      A <- Sen(GraupRad(90))
      escreva(A)
      
      
      //FUNÇÕES EM PORTUGOL
      // A <- Abs(-30)  == VALOR ABSOLUTO
      // A <- Exp(4, 3) == EXPONENCIAÇÃO
      // A <- RaizQ(81) == RAIZ QUADRADA, A <- Int(RaizQ(90)) 'APENAS A PARTE INTEIRA'
FimAlgoritmo
