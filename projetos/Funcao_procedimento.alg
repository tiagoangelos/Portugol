algoritmo "SOMAR 2 NUMEROS - PROCEDIMENTO"

var
   x, y: inteiro
   
procedimento soma(a, b: inteiro)
inicio
      escreval("RECEBI: ", a)
      escreval("RECEBI: ", b)
      escreval("A SOMA É: ", a+b)
fimprocedimento
inicio
      x <- 50
      y <- 30
      
      soma(x, y)
fimalgoritmo
